#include <iostream>
using namespace std;
/*
int main()
{
    cout<<"Enter no. of input: ";
    int n;
    int count=0;
    srand(time(0));
    cin>>n;
    unsigned short int arr[n];
    
    for (int i=0; i<n; i++)
    {
        arr[i]=rand();
    }
    
    for (int i=0; i<n; i++)
    {
        cout<<arr[i]<<"\n";
    }
    
    cout<<"\n";
    //insertion sort
    for (int j=1; j<n; j++)
    {
        int k=j-1;
        while (k>=0 && arr[k]>arr[j])
        {
            int temp = arr[j];
            arr[j] = arr[k];
            arr[k] = temp;
            j--;
            k--;
        }
        count++;
    }
    
    for (int i=0; i<n; i++)
    {
        cout<<arr[i]<<"\n";
    }
    
    cout<<"No. of comparisons: "<<count<<"\n";
    
    return 0;
}
 */
